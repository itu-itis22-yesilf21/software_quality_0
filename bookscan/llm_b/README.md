# LLM B — Gemini 3.1 Pro

Phase 2 artefacts generated by **LLM B = Gemini 3.1 Pro**.

Two prompt variants will live below this directory:

- `unmodified/` — output of the unmodified+combined prompt
- `edited/` — output of the edited+combined prompt

For Phase 1 outputs from the same model, see
`gemini_process/generated_code/Java_X/`.
